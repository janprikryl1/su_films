# Projekt 1

## Spuštění projektu

### Backend

- `cd api`
- `python manage.py runserver`

### Frontend (není potřeba spouštět, protože je sestavený a je přístupný přímo po spuštění backendu na http://localhost:8000/)

- `cd client`
- `npm install`
- `npm run dev`
